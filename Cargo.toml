[workspace]
members = ["macros"]

[package]
name = "evergreen_relations"
version = "0.3.0"
edition = "2024"

[dependencies]
evergreen_relations_macros = { path = "macros" }
bevy_ecs = { version = "0.18", default-features = false }
smallvec = { version = "1.13", features = ["const_generics"] }
